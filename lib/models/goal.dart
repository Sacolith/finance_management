//Goal blueprint used througout Application
class Goal{
  final String id;
  final String description;
  final double targetAmount;
  final double savedAmount;
  Goal({required this.id,required this.description,required this.savedAmount,required this.targetAmount});
}